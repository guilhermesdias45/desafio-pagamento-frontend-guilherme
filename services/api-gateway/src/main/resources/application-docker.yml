spring:
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST}
      port: ${SPRING_DATA_REDIS_PORT}
      password: ${SPRING_DATA_REDIS_PASSWORD}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 10
          min-idle: 2

# Rotas usam ${USER_SERVICE_URL}, ${PAYMENT_SERVICE_URL}, ${ORDER_SERVICE_URL}
# resolvidas pelas variáveis de ambiente do docker-compose — não redefinir aqui.
