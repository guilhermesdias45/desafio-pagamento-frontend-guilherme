spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
  # Mail auto-config disabled in tests to avoid SMTP connection attempts
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration
